import { View, StyleSheet, Text } from 'react-native';

const HireArtisanScreen = (): React.JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>HireArtisanScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default HireArtisanScreen;
