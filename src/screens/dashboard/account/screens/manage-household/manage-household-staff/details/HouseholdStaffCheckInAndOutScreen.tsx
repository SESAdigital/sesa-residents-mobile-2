import { View, StyleSheet, Text } from 'react-native';

const HouseholdStaffCheckInAndOutScreen = (): React.JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>HouseholdStaffCheckInAndOutScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default HouseholdStaffCheckInAndOutScreen;
