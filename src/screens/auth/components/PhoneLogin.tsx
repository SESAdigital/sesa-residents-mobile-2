import { View, StyleSheet, Text } from 'react-native';

const PhoneLogin = (): React.ReactNode => {
  return (
    <View style={styles.container}>
      <Text>PhoneLogin</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default PhoneLogin;
