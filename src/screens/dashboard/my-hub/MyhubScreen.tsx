import { View, StyleSheet, Text } from 'react-native';

const MyhubScreen = (): React.ReactNode => {
  return (
    <View style={styles.container}>
      <Text>MyhubScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default MyhubScreen;
