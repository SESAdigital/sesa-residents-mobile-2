import { View, StyleSheet, Text } from 'react-native';

const MyQRCodeScreen = (): React.JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>MyQRCodeScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default MyQRCodeScreen;
