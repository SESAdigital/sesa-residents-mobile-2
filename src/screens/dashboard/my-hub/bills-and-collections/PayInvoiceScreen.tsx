import { View, StyleSheet, Text } from 'react-native';

const PayInvoiceScreen = (): React.JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>PayInvoiceScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default PayInvoiceScreen;
