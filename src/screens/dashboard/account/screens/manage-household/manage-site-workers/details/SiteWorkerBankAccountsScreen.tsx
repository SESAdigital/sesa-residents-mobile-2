import { View, StyleSheet, Text } from 'react-native';

const SiteWorkerBankAccountsScreen = (): React.JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>SiteWorkerBankAccountsScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default SiteWorkerBankAccountsScreen;
