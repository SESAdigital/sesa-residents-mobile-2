import { View, StyleSheet, Text } from 'react-native';

const BuyPowerScreen = (): React.JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>BuyPowerScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default BuyPowerScreen;
