import { View, StyleSheet, Text } from 'react-native';

const NotificationIconMapper = (): React.JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>NotificationIconMapper</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default NotificationIconMapper;
