import { View, StyleSheet, Text } from 'react-native';

const ManageRFIDsScreen = (): React.JSX.Element => {
  return (
    <View style={styles.container}>
      <Text>ManageRFIDsScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});

export default ManageRFIDsScreen;
